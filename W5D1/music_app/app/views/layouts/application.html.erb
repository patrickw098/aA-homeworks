<!DOCTYPE html>
<html>
  <head>
    <title>MusicApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header align="center">
      Music App! Yeaaaahhhhh~~
    <% if logged_in? %>
    <br>
    <h1>Welcome, <%= current_user.email%>
    <br>
    <form action="<%=session_url%>" method="post">
      <%= auth_token %>
      <input type="hidden" name="_method" value="delete">
      <input type="submit" value="Log Out">
    </form>
    </h1>
    <% else %>
    <div class="nav" style="float:right">
      <a style="background-color:lightblue; padding:5px 10px; border-radius:30px; border: 1px solid black" href="<%=new_user_url%>">Sign Up</a>
      <a style="background-color:lightblue; padding:5px 10px; border-radius:30px; border: 1px solid black" href="<%=new_session_url%>">Log In</a>
    <% end %>
    </div>
  </header>

    <ul>
      <% if flash[:errors] %>
        <% flash[:errors].each do |error| %>
          <li> <%= error %> </li>
          <% end %>
      <% end %>
    </ul>
    <%= yield %>
  </body>
</html>
